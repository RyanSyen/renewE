/** @type {import('next').NextConfig} */
const nextConfig = {
  //   publicRuntimeConfig: {
  //     // Replace `my-app` with the name of your Next.js app
  //     assetPrefix: process.env.NODE_ENV === "production" ? "/renewe" : "",
  //   },
};

module.exports = nextConfig;
